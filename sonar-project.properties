# ======================================================
# SonarCloud Configuration for Python Project
# ======================================================

# 🏷️ SonarCloud project identification
sonar.organization=hemanthkumarg0808-dotcom
sonar.projectKey=hemanthkumarg0808-dotcom_hemanth
sonar.projectName=hemanth
sonar.projectVersion=1.0

# 📁 Source code location
# Update the folder path if your .py files are in a subdirectory (for example: hemanth/)
sonar.sources=.

# 🧠 Code encoding
sonar.sourceEncoding=UTF-8

# 🚫 Exclusions (skip virtual environments, caches, etc.)
sonar.exclusions=**/venv/**,**/__pycache__/**,**/migrations/**,**/tests/**

# ✅ (Optional) Include test files if you have them
sonar.tests=.
sonar.test.inclusions=**/test_*.py,**/*_test.py

# 🧪 (Optional) Coverage report file (if you add pytest + coverage later)
# sonar.python.coverage.reportPaths=coverage.xml

# ======================================================
# End of file
# ======================================================
